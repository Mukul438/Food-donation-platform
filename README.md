# AI for Food Waste Reduction

Steps to run backend:
1. Create virtual environment and install dependencies.
2. Train the model: `python ml/train_baseline.py`
3. Start API: `uvicorn api.main:app --reload`
